# Agentic RAG - requirements

## Core app
streamlit>=1.18
PyPDF2>=3.0
python-dotenv>=1.0
nest-asyncio>=1.5
requests>=2.28

## Numeric & ML
numpy>=1.24
scikit-learn>=1.2
joblib>=1.2
tqdm>=4.65

## Tokenization (optional but recommended for token-aware chunking)
tiktoken>=0.4.0

## LLM / embeddings wrappers (may require source install if not on PyPI)
langchain-google-genai>=0.1
langchain-community>=0.1
langchain-core>=0.1
langgraph>=0.1

## Vector store / Chroma backend
chromadb>=0.3

## OAuth / HTTP helpers
authlib>=1.2
python-multipart>=0.0.5

## OCR / scanned PDF support (optional — needs system deps)
pillow>=9.0
pdf2image>=1.16
pytesseract>=0.3.10

## Table / document extras (optional)
pdfplumber>=0.7
python-docx>=0.8.11
python-pptx>=0.6.21
beautifulsoup4>=4.12.2
rank-bm25>=0.2.2

## Optional DB / persistence connectors (only if you plan to use them)
cassandra-driver>=3.27.0
cryptography>=40.0
